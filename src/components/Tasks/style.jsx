import styled from "styled-components";

export const StyleTask = styled.div`
    p{
        color: white;
        
            &--completed {
            text-decoration: line-through;
            opacity: 0.7;
	}
    }
`